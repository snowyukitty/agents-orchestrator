{
  "name": "agents-orchestrator",
  "version": "0.1.8",
  "description": "Visual AI Agent Automation Orchestrator - Block-based CLI automation tool",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --dev",
    "smoke": "electron . --smoke-test",
    "test": "electron . --self-test",
    "check": "node --check main.js && node --check preload.js && node --check src/js/app.js && node --check src/js/engine.js && node --check src/js/blocks.js && node --check src/js/templates.js && node --check src/js/schedule.js && node --check scripts/build-icons.js && node --check scripts/create-icon.js",
    "icons": "electron scripts/build-icons.js",
    "build": "electron-packager . AgentsOrchestrator --platform=win32 --arch=x64 --overwrite --out=dist --icon=src/assets/icon.ico --ignore=\"(dist|scripts|mcps|icon-source\\.png|\\.gitignore)\" --asar.unpackDir=node_modules/node-pty"
  },
  "devDependencies": {
    "electron": "^35.0.0",
    "electron-packager": "^17.1.2"
  },
  "dependencies": {
    "node-pty": "^1.1.0",
    "sortablejs": "^1.15.6",
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.8.0"
  }
}
